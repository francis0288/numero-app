{
  "version": 2,
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @numero-app/web exec prisma generate && pnpm --filter @numero-app/web build",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs"
}
